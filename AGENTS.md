# AGENTS.md

## Cursor Cloud specific instructions

This repository ("TestC") is currently an empty scaffold with only a `README.md`. There are no application services, dependencies, tests, or build steps at this time.

- **No package manager or lockfile** — no `npm install`, `pip install`, etc. needed.
- **No dev server or build** — no `pnpm dev`, `npm start`, etc. available.
- **No tests or lint** — no test runner or linter configured.

When code is added, update this section with the relevant dev commands, service descriptions, and any non-obvious caveats discovered during setup.
